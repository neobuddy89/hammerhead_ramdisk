BB=/sbin/busybox;

if [[ ! -z $1 ]]; then
	$BB echo $1 > /sys/module/cpu_boost/parameters/input_boost_freq;
	$BB echo $1 > /res/synapse/files/input_boost_freq;
fi

echo `cat /res/synapse/files/input_boost_freq`
